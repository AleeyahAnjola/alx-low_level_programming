#include "main.h"

/**
 * largest_number - returning the largest of 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int largest_number(int a, int b, int c)
{
	int largestnumber;

	largestnumber = (a > b) ? a : b;
	largestnumber = (largestnumber > c) ? largest : c;
	return (largest);
}
